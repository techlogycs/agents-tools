# schema-multi-model-intentional

Use SurrealDB's multi-model features deliberately, not all at once.

## Apply when

- Introducing graph, search, vector, or document-specific features.

## Do

- Choose the smallest feature set that solves the workload cleanly.

## Avoid

- Adopting features for novelty without a concrete query need.
