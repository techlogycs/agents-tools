# anti-multi-model-hype

Do not use graph, search, vector, and events unless the workload justifies them.

## Why

- Feature sprawl raises system complexity faster than it raises value.

## Prefer

- The smallest SurrealDB feature set that solves the real query path.
