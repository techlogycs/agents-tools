# perf-vector-index

Use vector indexes for embedding similarity and keep dimensions consistent.

## Apply when

- Building similarity search or retrieval features.

## Do

- Ensure the vector shape and index configuration match the model output.

## Avoid

- Mixing embeddings with inconsistent dimensions or distance assumptions.
