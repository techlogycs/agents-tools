# rt-events-small

Keep event work small and fast inside the database.

## Apply when

- Adding logic to `DEFINE EVENT` handlers.

## Do

- Keep event behavior narrow, deterministic, and bounded.

## Avoid

- Turning events into large workflow engines.
